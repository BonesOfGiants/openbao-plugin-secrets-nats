# Infrastructure as Code

This plugin is designed to work seamlessly with infrastructure as code (IaC) systems like Terraform/OpenTofu or Pulumi.

The declarative interface allows the creation of a fully authenticated, multi-tenant NATS cluster in a single pass.

<!--

- designed to work well with iac
- able to bring up a fully authenticated server in a single-pass of iac
- pulumi sample using vault provider
- opentofu/terraform sample using vault provider

-->